# Experiments

Each experiment should be reproducible and include a dated protocol, dataset identity, inclusion and exclusion rules, estimator and parameter versions, baselines, metrics, environment, random seeds, results, limitations, and links to generated artifacts.

Prefer registering the analysis plan before inspecting outcomes. Never add restricted data to the repository.
